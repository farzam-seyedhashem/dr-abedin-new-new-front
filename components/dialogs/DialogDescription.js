export default function DialogDescription({children}){
    return(
        <p className={"text-on-surface-variant-light dark:text-on-surface-variant-dark mb-6 text-body-medium "}>
            {children}
        </p>
        )
}