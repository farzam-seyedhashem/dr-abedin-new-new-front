import localFont from 'next/font/local'


// define a custom local font where GreatVibes-Regular.ttf is stored in the styles folder
const IranYekan = localFont({ src: './fonts/IRANYekanXVF.woff2',variable: '--iran-yekan' })

export {IranYekan}